name: Github Actions
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up OpenJDK
        uses: actions/setup-java@v1
        with:
          java-version: '12'
      - name: Build with Gradle
        run: make build
